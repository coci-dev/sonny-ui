export const COPY_PASTE_EN = {
  title: 'Copy & Paste',
  description: 'Own the code. Copy components directly into your project.',
  philosophy: 'Philosophy',
  philosophyDesc: 'SonnyUI follows the same philosophy as shadcn/ui: you can use it as an npm package, or copy the source code directly into your project. This gives you full control to customize components without fighting against library abstractions.',
  automatic: 'Automatic (recommended)',
  automaticDesc: 'Use the schematic to copy a component automatically. It handles file copying, import rewrites, and the <code class="font-mono text-xs bg-muted px-1 py-0.5 rounded">cn()</code> utility for you:',
  automaticOptions: 'Options:',
  automaticOptionsList: [
    '<code class="font-mono text-xs bg-muted px-1 py-0.5 rounded">--path</code> — target directory (default: <code class="font-mono text-xs bg-muted px-1 py-0.5 rounded">src/app/ui</code>)',
    '<code class="font-mono text-xs bg-muted px-1 py-0.5 rounded">--prefix</code> — selector prefix (default: <code class="font-mono text-xs bg-muted px-1 py-0.5 rounded">sny</code>)',
    '<code class="font-mono text-xs bg-muted px-1 py-0.5 rounded">--skipTests</code> — don\'t copy test files',
  ],
  manual: 'Manual',
  manualSteps: [
    { strong: 'Copy the source files', text: ' — Each component lives in its own folder with a directive, variants, and index file.' },
    { strong: 'Copy shared utilities', text: ' — You\'ll need the <code class="font-mono text-xs bg-muted px-1 py-0.5 rounded">cn()</code> utility and the theme CSS.' },
    { strong: 'Update imports', text: ' — Change import paths to point to your local copies.' },
  ],
  exampleButton: 'Example: Button',
  exampleButtonDesc: 'Copy these files into your project:',
  cnUtility: 'The cn() utility',
  buttonVariants: 'Button variants',
  buttonDirective: 'Button directive',
  usageTitle: 'Usage',
  usageDesc: 'Once you\'ve copied the component (automatically or manually), import it and use it in your templates:',
  importDirective: '1. Import the directive',
  useTemplate: '2. Use it in your template',
  availableVariants: 'Available variants: <code class="font-mono text-xs bg-muted px-1 py-0.5 rounded">default</code>, <code class="font-mono text-xs bg-muted px-1 py-0.5 rounded">destructive</code>, <code class="font-mono text-xs bg-muted px-1 py-0.5 rounded">outline</code>, <code class="font-mono text-xs bg-muted px-1 py-0.5 rounded">secondary</code>, <code class="font-mono text-xs bg-muted px-1 py-0.5 rounded">ghost</code>, <code class="font-mono text-xs bg-muted px-1 py-0.5 rounded">link</code>. Sizes: <code class="font-mono text-xs bg-muted px-1 py-0.5 rounded">sm</code>, <code class="font-mono text-xs bg-muted px-1 py-0.5 rounded">md</code>, <code class="font-mono text-xs bg-muted px-1 py-0.5 rounded">lg</code>, <code class="font-mono text-xs bg-muted px-1 py-0.5 rounded">icon</code>.',
  dependencies: 'Dependencies',
  dependenciesDesc: 'If copying components, you still need these packages:',
};
